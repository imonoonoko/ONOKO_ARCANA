# Purpose

ONOKO ARCANA GitHub Release downloads should feel like a normal Windows desktop app preview. A user should not need to manually create a shortcut or run an extra icon helper just to see the ONOKO ARCANA icon.

The next release should make the installer the recommended artifact. The installer should create shortcuts automatically and those shortcuts should use the generated `.ico` icon.
