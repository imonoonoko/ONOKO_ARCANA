# Purpose

Connect the Phase 4 spread runtime to the actual play surface so the user can select a multi-card spread and see multiple cards in the running Unreal window.

The immediate target is `three_card_past_present_future`.

This slice should bridge the gap between the commandlet-proven runtime and the visible game table without attempting the final full tarot UI.
