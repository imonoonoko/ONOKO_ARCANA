# UI Prompt

For this implementation slice, the play screen should remain functional and readable:

- Keep the current dark HUD panel.
- Show active spread name, slot list, selected position, and progress.
- Use the table area for real card actors.
- Place the three-card spread as Past / Present / Future from left to right.
- Avoid decorative UI refactors until the runtime is proven.

Future visual direction can use the richer ONOKO ARCANA concepts already generated under `assets/generated/reports/`.
